
  # Recreate TeamConnect Interface

  This is a code bundle for Recreate TeamConnect Interface. The original project is available at https://www.figma.com/design/kEiq30nLNJs5J8Id56wPeo/Recreate-TeamConnect-Interface.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  